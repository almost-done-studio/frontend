# Copy to .env and adjust. Never commit .env.

VITE_API_URL=http://localhost:3000

# Capacitor appId / appName belong in capacitor.config.ts (not Vite env).
# See .claude/skills/capacitor or .cursor/skills/capacitor when adding native builds.
